<blueprint
        xmlns    = "http://www.osgi.org/xmlns/blueprint/v1.0.0"
        xmlns:cm = "http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0">

    <!-- ******************************************************************* -->
    <!--                                                                     -->
    <!-- ******************************************************************* -->

    <cm:property-placeholder persistent-id="com.github.lburgazzoli.cluster.hz" update-strategy="reload">
        <cm:default-properties>
            <cm:property name="node.id"                    value="ha"/>
            <cm:property name="node.groups"                value=""/>
            <cm:property name="node.leader.eligible"       value="false"/>
            <cm:property name="node.leader.check.delay"    value="30"/>
            <cm:property name="node.leader.check.interval" value="10"/>
        </cm:default-properties>
    </cm:property-placeholder>

    <!-- ******************************************************************* -->
    <!--                                                                     -->
    <!-- ******************************************************************* -->

    <reference
        id        = "karaf-hazelcast-manager"
        interface = "com.github.lburgazzoli.karaf.hazelcast.IHazelcastManager"
        filter    = "(osgi.jndi.service.name=karaf-hazelcast-manager)"/>

    <!-- ******************************************************************* -->
    <!--                                                                     -->
    <!-- ******************************************************************* -->

    <bean id             = "karaf-cluster-agent"
          class          = "com.github.lburgazzoli.karaf.hazelcast.cluster.ClusterAgent"
          init-method    = "init"
          destroy-method = "destroy"
          scope          = "singleton">
        <property name="nodeId"           value="${node.id}"/>
        <property name="nodeGroups"       value="${node.groups}"/>
        <property name="bundleContext"    ref="blueprintBundleContext"/>
        <property name="hazelcastManager" ref="karaf-hazelcast-manager"/>
    </bean>

    <reference-list
        id="lb-service-reference-listerner"
        interface="com.github.lburgazzoli.cluster.IClusteredService"
        availability="optional">
        <reference-listener
            ref="karaf-cluster-agent"
            bind-method="bind"
            unbind-method="unbind"/>
    </reference-list>

    <service ref="karaf-cluster-agent">
        <interfaces>
            <value>com.github.lburgazzoli.cluster.IClusterAgent</value>
        </interfaces>
        <service-properties>
            <entry key   = "osgi.jndi.service.name"
                   value = "karaf-cluster-agent"/>
        </service-properties>
    </service>

    <!-- ******************************************************************* -->
    <!-- COMMANDS                                                            -->
    <!-- ******************************************************************* -->

    <command-bundle xmlns="http://karaf.apache.org/xmlns/shell/v1.0.0">
        <command name="cluster/node-list">
            <action class="com.github.lburgazzoli.karaf.hazelcast.cluster.cmd.NodeList">
                <property name="service" ref="karaf-cluster-agent"/>
            </action>
        </command>
        <command name="cluster/group-list">
            <action class="com.github.lburgazzoli.karaf.hazelcast.cluster.cmd.GroupList">
                <property name="service" ref="karaf-cluster-agent"/>
            </action>
        </command>
    </command-bundle>

</blueprint>
